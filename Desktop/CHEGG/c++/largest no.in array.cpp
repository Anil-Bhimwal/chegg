#include<iostream>
using namespace std;
/*int largest(int[],int);*/
main()
{int ch=20;
cout<<ch<<ch++<<ch;/* char ch;
int arr[50],greatest,i;
for( i=0;i<50;i++)
    {
        cout<<"element no."<<(i+1)<<endl;
        cin>>arr[i];
        cout<<"more elements?"<<"y/n"<<endl;
        cin>>ch;
        if(ch!='y')
            break;
    }
    if (i<50)
        i++;
        greatest=largest(arr,i);
        cout<<"the largest no.of array:"<<greatest<<endl;
        return 0;
}
int largest(int arr[50],int n )
{
    int max=arr[0];
    for( int j=1;j<n;j++)
    {
        if (arr[j]>max)
            max=arr[j];
    }
    return max;
    */
return 0;
}
